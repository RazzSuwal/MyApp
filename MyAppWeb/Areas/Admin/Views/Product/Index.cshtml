@model MyApp.Models.ViewModels.ProductVM
@{
    ViewData["Title"] = "Index";
}

<div>
    <div class="col-6">
        <h1 class="mt-5">Product</h1>
    </div>
    <div class="col-6">
        <a class="nav-link" asp-controller="Product" asp-action="CreateUpdate"> Add Products</a>
    </div>
</div>
<table id="myTable" class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Product Name
            </th>
            <th>
                Discription
            </th>
            <th>
                Price
            </th>
            <th>
                Category
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
   @* @foreach(var items in Model.products){
    <tbody>
        <tr>
                <td>
                    @items.Name
                </td>
                <td>
                    @items.DisplayOrder
                </td>
                <td role="group">
                    <a asp-action="CreateUpdate" asp-controller="Product" asp-route-id="@items.Id">Edit</a>
                    <a asp-action="Delete" asp-controller="Product" asp-route-id="@items.Id">Delete</a>
                </td>
        </tr>
    </tbody>
    }*@

</table>

@section scripts{
    <script src ="~/js/Product.js"> </script>
}
